常大校园快递收发系统 - 前端

基于 Vue 3 + TypeScript 开发的现代化校园快递收发系统前端应用（课程实训项目）。

##  功能特性

### 用户端功能
- 用户注册与登录
- 我的包裹查看与管理
- 包裹取件
- 在线寄件服务
- 个人信息管理

### 管理员功能
- 管理控制台（数据统计）
- 包裹管理（查看、编辑、更新状态）
- 用户管理（查看所有用户）
- 包裹入库
- 实时数据刷新

##  技术栈

- **前端框架**: Vue 3 (Composition API)
- **开发语言**: TypeScript
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP客户端**: Axios
- **构建工具**: Vite
- **代码规范**: ESLint + Prettier

##  项目结构

```
web/
├── src/
│   ├── api/              # API请求封装
│   │   ├── client.ts     # Axios实例配置
│   │   └── index.ts      # API接口定义
│   ├── router/           # 路由配置
│   │   └── index.ts      # 路由定义及导航守卫
│   ├── stores/           # Pinia状态管理
│   │   ├── auth.ts       # 认证状态
│   │   └── counter.ts    # 包裹状态
│   ├── types/            # TypeScript类型定义
│   │   └── index.ts      # 全局类型
│   ├── views/            # 页面组件
│   │   ├── HomePage.vue          # 首页
│   │   ├── LoginPage.vue         # 登录页
│   │   ├── RegisterPage.vue      # 注册页
│   │   ├── user/                 # 用户端页面
│   │   └── admin/                # 管理员页面
│   ├── App.vue           # 根组件
│   └── main.ts           # 应用入口
└── package.json          # 依赖配置
```

## 🚀 快速开始

### 环境要求

- Node.js >= 20.19.0 或 >= 22.12.0
- npm 或 pnpm

### 安装依赖

```sh
npm install
```

### 开发运行

```sh
npm run dev
```

应用将在 [http://localhost:7999](http://localhost:7999) 启动

### 构建生产版本

```sh
npm run build
```


##  后端API配置

后端API地址在开发环境下通过 Vite 代理转发，配置在 `web/.env`：

```typescript
Server_IP="输入你的后端地址"
```


##  路由结构

### 公开路由
- `/` - 首页
- `/login` - 登录页
- `/register` - 注册页

### 用户路由 (需要认证)
- `/user` - 用户仪表盘
- `/user/packs` - 我的包裹
- `/user/mail` - 寄件服务
- `/user/profile` - 个人信息

### 管理员路由 (需要管理员权限)
- `/admin` - 管理控制台
- `/admin/packs` - 包裹管理
- `/admin/users` - 用户管理
- `/admin/checkin` - 包裹入库

##  UI设计特点

1. **现代简洁** - 采用现代化设计语言，界面简洁美观
2. **多彩渐变** - 使用渐变色彩，视觉效果丰富
3. **响应式布局** - 完美支持各种屏幕尺寸
4. **流畅动画** - 丰富的过渡动画和交互效果
5. **统一风格** - 用户端和管理端有明显区分但保持统一的设计语言


##  开发说明

### Type Support for `.vue` Imports in TS

TypeScript cannot handle type information for `.vue` imports by default, so we replace the `tsc` CLI with `vue-tsc` for type checking. In editors, we need [Volar](https://marketplace.visualstudio.com/items?itemName=Vue.volar) to make the TypeScript language service aware of `.vue` types.

### Recommended IDE Setup

[VS Code](https://code.visualstudio.com/) + [Vue (Official)](https://marketplace.visualstudio.com/items?itemName=Vue.volar) (and disable Vetur).


**祝使用愉快！** 🎉

